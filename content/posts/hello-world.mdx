---
title: "hello, world"
description: "the first post — and a quick tour of what this site can do."
date: "2026-04-28"
tags: ["meta"]
math: false
accent: "sage"
---

this is the first post on the site. mostly i'm using it to test that everything renders the
way i want.

## what works here

a plain paragraph with **bold** and _italic_ and `inline code`, plus a [link](https://example.com)
that gets a quiet highlight on hover.

```python
# fenced code blocks have syntax highlighting and a copy button
def hello(name: str) -> str:
    return f"hello, {name}"
```

> blockquotes are set in the display serif. they're for moments where the prose wants to
> slow down a little.

<Callout variant="note" title="callouts">
  there are four variants — `info`, `note`, `warn`, and `idea` — for the kinds of sidebar
  comments that don't belong in the main flow.
</Callout>

## what's next

i'll write more here when there's something worth saying. for the schema and all the
available components, see `CONTENT.md` in the repo.
